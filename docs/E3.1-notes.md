# E3.1 Notes — Landing Page V1

## What Changed
- Added landing shortcode:
  - `[oliveb2b_landing]`
- Added styled landing sections:
  - Hero with primary CTAs
  - Role blocks (Buyer/Supplier/Professional)
  - Logged-in quick action strip
  - Core value feature grid
- Seeder now creates `Home` page using `[oliveb2b_landing]`.
- Seeder sets `Home` as WordPress front page.

## How To Test
1. Reseed:
   - `docker compose run --rm --user 0:0 --entrypoint wp wpcli oliveb2b seed --reset --allow-root --path=/var/www/html`
2. Visit:
   - `http://localhost:8080/`
3. Confirm:
   - Hero section and CTAs render
   - Role and feature cards render
   - Logged-in users see quick action strip
